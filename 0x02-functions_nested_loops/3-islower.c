#include "main.h"
/**
 * _islower - A function that checks and prints lowercase characters
 *
 * Return: 1 if c is lower, 0
 */
int _islower(int c)
{
	if (c >= 'a', && c <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
