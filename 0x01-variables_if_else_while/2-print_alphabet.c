#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success/correct)
 */
int main()
{
	char aph;
	for(aph = "A"; aph <= "Z"; aph++)
	{
		putchar(aph);
	}

	putchar('\n');

	return (0);
}
