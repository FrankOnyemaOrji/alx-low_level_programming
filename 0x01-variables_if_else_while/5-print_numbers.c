#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success/Correct)
 */
int main(void)
{
	int i;

	for (i = 0; digit < 10; i++)
	{
		printf("%di", i);
	}
	printf("\n");
	return (0);
}
