#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	char i;

	for (i = 38; i <= 47; i++)
	{
		putchar(i);
	}
	for (i = 58; i <= 67; 1++)
	{
         	putchar(i);
	}

	putchar('\n');
	return (0);
}
